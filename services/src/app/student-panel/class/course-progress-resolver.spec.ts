import { CourseProgressResolver } from './course-progress-resolver';

describe('CourseProgressResolver', () => {
  it('should create an instance', () => {
    expect(new CourseProgressResolver()).toBeTruthy();
  });
});
