import { Modulefileprogress } from './modulefileprogress';

describe('Modulefileprogress', () => {
  it('should create an instance', () => {
    expect(new Modulefileprogress()).toBeTruthy();
  });
});
