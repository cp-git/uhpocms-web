import { AdvFilterPipe } from './adv-filter.pipe';

describe('AdvFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdvFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
